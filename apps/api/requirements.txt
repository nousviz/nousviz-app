fastapi>=0.115.0
uvicorn[standard]>=0.34.0
gunicorn>=21.2
psycopg2-binary>=2.9.0
httpx>=0.27.0
pyyaml>=6.0
python-dotenv>=1.0.0
pydantic>=2.0.0
cryptography>=42.0.0
bcrypt>=4.0.0
croniter>=2.0,<3.0

jinja2>=3.1.0
markdown>=3.7
python-multipart>=0.0.12

# P201 (v0.9.0): install nousviz-sdk as editable from the sdk/ directory.
# Without this, plugins that do `from nousviz_sdk import X` raise
# ModuleNotFoundError and their routes silently fail to register.
# Path is relative to the repo root; `pip install -r apps/api/requirements.txt`
# must run from the repo root (setup.sh and deploy-local.sh do).
-e ./sdk

